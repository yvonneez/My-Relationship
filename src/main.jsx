import React from "react";
import ReactDOM from "react-dom/client";
import RelationshipApp from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RelationshipApp />
  </React.StrictMode>
);
